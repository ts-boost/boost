{
    "extends": "../../tsconfig.build.json",
    "compilerOptions": {
        "declarationDir": "./lib" /* Specify output directory for generated declaration files. */,
        "outDir": "./lib" /* Redirect output structure to the directory. */,
        "rootDir": "./src" /* Specifies the root directory of input files. Use to control the output directory structure with --outDir. */
    },
    "exclude": ["src/**/*.__test__.ts"],
    "include": ["src/**/*"]
}
